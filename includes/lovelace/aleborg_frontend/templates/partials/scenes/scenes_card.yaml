# lovelace_gen

# Scenes
#############################################
type: custom:auto-entities
card:
  type: custom:mushroom-chips-card
  card_mod: !include styles/scenes_card_card_mod.yaml
card_param: chips
filter:
  include: !include
    - auto-entities/include.yaml
    - title: {{title}}
  exclude: !include auto-entities/exclude.yaml
